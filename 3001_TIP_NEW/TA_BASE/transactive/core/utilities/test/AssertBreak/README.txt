Brad Cleaver
20/02/2004

A simple test to determine if TA_ASSERT will cause the debugger to break when TA_ASSERT_FAIL=BREAK.

To run the test:
1. Load the AssertBreak workspace.
2. Start a debug session.
3. Debugger should automatically break on line 45 of main.cpp.
